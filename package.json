{
  "name": "squarespace-site-styles-search",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:code-injection": "node scripts/build-code-injection.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm test && node --check site-styles-search.js && node scripts/build-code-injection.mjs --check"
  }
}
